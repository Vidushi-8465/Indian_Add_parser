[project]
name = "indian-add-geo"
version = "0.1.0"
description = "Indian Address Geocoding and Resolution Engine"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
